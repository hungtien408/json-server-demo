{
  "name": "json-server-kats",
  "version": "1.0.0",
  "description": "use json-server kats",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && node main.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Tien Nguyen",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.13"
  }
}
